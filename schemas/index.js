const { contactSchema, contactFavoriteSchema } = require('./contactSchema')

const { registerUserSchema, loginUserSchema, emailSchema } = require('./userSchema')

module.exports =  { contactSchema, contactFavoriteSchema, registerUserSchema, loginUserSchema, emailSchema };